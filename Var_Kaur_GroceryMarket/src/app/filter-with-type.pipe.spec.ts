import { FilterWithTypePipe } from './filter-with-type.pipe';

describe('FilterWithTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterWithTypePipe();
    expect(pipe).toBeTruthy();
  });
});
