import { FilterWithContentPipe } from './filter-with-content.pipe';

describe('FilterWithContentPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterWithContentPipe();
    expect(pipe).toBeTruthy();
  });
  
});
